# Data

Temporary local app data goes here while features are being wired to Sanity and the mobile API.

The current hardcoded motivation entries in `App.tsx` can move here during the first refactor.

